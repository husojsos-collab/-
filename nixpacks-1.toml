# هذا الملف يخلي Railway يثبت "ffmpeg" كبرنامج نظام (system package) وقت بناء
# المشروع، بغض النظر عن حزمة ffmpeg-static اللي بـ npm (اللي فشلت تجيب نسخة
# شغالة على هذا السيرفر). حط هذا الملف بجذر المشروع (نفس مكان package.json).
#
# بعد ما تضيفه، لازم تعمل Redeploy للمشروع من Railway عشان يطبق التغيير.

[phases.setup]
nixPkgs = ["ffmpeg"]
